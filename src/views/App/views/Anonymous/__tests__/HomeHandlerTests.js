import { expect } from 'chai';
import { it, describe } from 'mocha';

describe('Home tests', () => {
  describe('Home renders', () => {
    it('renders a header', () => {
      expect('test').to.equal('test');
    });
  });
});
